# Python
.venv/
__pycache__/
*.pyc

# Environment
.env

# Secrets in this directory (see also repo-root .gitignore for keys.json / KEYS.js)
keys.json

# Audio uploads (participant reply blobs under static/audio/)
static/audio/

# Data logs (`*.csv` sharing is intentional for some experiments; uncomment to ignore logs locally)
#*.csv

# Mac
.DS_Store
