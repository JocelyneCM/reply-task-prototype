# Requirements for running inside the ROCm Docker image.
# NOTE: `torch` is intentionally omitted because the ROCm base image
# should provide a PyTorch build compatible with the system's ROCm.
pandas>=1.3.0
datasets>=2.0.0
transformers>=4.30.0
# torch is provided by the base ROCm image
numpy>=1.21.0
scikit-learn>=1.0.0
Flask>=3.0.0
nltk>=3.9.1
beautifulsoup4>=4.11.1
